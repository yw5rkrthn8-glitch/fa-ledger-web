# 记账 Web 需求规格（SPEC）

## 1. Excel 数据结构
- 文件名：在Data目录下的FA文件 
- Sheet 名：Records
- 字段清单（按列顺序）：
  - 字段名 | 类型(文本/数字/日期/枚举) | 必填(Y/N) | 示例 | 备注
  - Date, 支出/收入，类型（餐饮，聚餐，教育，日常开支，保姆，etc）， 金额，其他备注
- 主键字段：
- 唯一约束：
- 默认值规则：
- 导入时校验规则（例如金额 >= 0）：
- 异常数据处理（跳过/报错/标记）：

## 2. 页面呈现要求
- 页面列表：按月来呈现收入和支出情况，包含金额，可类型的金额等，用可视化的方式
- 每个页面包含模块：
- 列表显示字段与顺序：
- 默认排序规则：
- 筛选条件：
- 汇总指标：
- 图表需求（如不需要可写“无”）：

## 3. 互动要求
- 新增记录流程： 保存到Excel文件里
- 编辑记录流程：
- 删除记录流程：
- Load Excel 行为（覆盖/追加）：追加
- Save Excel 行为（保存到选中文件/另存为）：保存到选择文件
- 自动保存触发时机（新增后/编辑后/删除后）：新增后
- 文件冲突处理（文件被外部修改时怎么处理）：
- 失败提示文案要求：

## 4. 非功能要求
- 支持浏览器范围： Safri, Chorme, firfox, etc.
- 是否需要离线可用：
- 性能要求：
- 权限与安全要求：

## 5. 验收标准
1. 能够增删查询，能够保存；
2. 页面显示清晰；
3. 
4. 
5. 
